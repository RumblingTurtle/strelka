PrintPackageHeader(robots)

SET(STRELKA_ROBOTS_DEPENDENCY_HEADERS 
    ${Eigen_INCLUDE_DIRS};
    ${STRELKA_MESSAGES_HEADERS};
    ${STRELKA_COMMON_HEADERS};
    ${CMAKE_CURRENT_SOURCE_DIR}/include;
)

FILE(GLOB_RECURSE STRELKA_ROBOTS_SOURCES ${CMAKE_CURRENT_SOURCE_DIR}/src/*.cpp)


set(STRELKA_ROBOTS_HEADERS
    ${CMAKE_CURRENT_SOURCE_DIR}/include
    PARENT_SCOPE
)

add_library(strelka_robots ${STRELKA_ROBOTS_SOURCES})
target_include_directories(strelka_robots PUBLIC ${STRELKA_ROBOTS_DEPENDENCY_HEADERS})

