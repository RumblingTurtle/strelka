+ WBIC

- Local planner
    Ideally a builder LocalPlanner<FootPlanner,BodyTrajectoryPlanner,Sequencer>();
    + Gait scheduler
    + Gait sequencer
        + Common interface
    - Body trajectory planner
        + Common interface
        - Lowpass filter for pitch and height
    + Foot planner
        + Common interface
    + MPC
    - Move A1 constants into UnitreeA1

- Code structure
    - Ensure lcm.good()
    + lcm.handle() == 0
    - Timeouts?
    - Sim/Hardware bridge to lcm
    - Decouple strelka and ros entirely(at least for now)
    - Make lcm message names more comprehensive 

- Testing
    - add gtest to deps
    - write actual tests

- State estimation
    - Multinomial Moving Window filter
    - External odometry
    - Resetable observer
    + Slowdown estimator for simulation 

- Build system
    - Macros for packages
    + Automatic source file search
            FILE(GLOB_RECURSE STRELKA_CONTROL_WBIC_SOURCES *.cpp)

- Elevation mapping???

+ Replace defines with constants
+ Write high level abstraction Robot() -> A1Robot(RobotState.lcm) 